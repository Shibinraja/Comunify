import { ToastContainer } from "react-toastify";
import "./Toast.css";

const Toast = () => {
  return (
    <div className="toast">
      <ToastContainer />
    </div>
  );
};

export default Toast;
